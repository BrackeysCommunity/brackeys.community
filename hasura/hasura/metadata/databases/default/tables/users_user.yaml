table:
  name: user
  schema: users
array_relationships:
  - name: userRoles
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_role
          schema: users
