table:
  name: profile
  schema: collab
array_relationships:
  - name: alerts
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: alert
          schema: collab
  - name: auditLogs
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: audit_log
          schema: collab
  - name: blockedUsers
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: blocked_user
          schema: collab
  - name: bookmarks
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: bookmark
          schema: collab
  - name: posts
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: post
          schema: collab
  - name: reports
    using:
      foreign_key_constraint_on:
        column: reported_by_profile_id
        table:
          name: report
          schema: collab
  - name: reportsByReportedProfileId
    using:
      foreign_key_constraint_on:
        column: reported_profile_id
        table:
          name: report
          schema: collab
  - name: responses
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: response
          schema: collab
