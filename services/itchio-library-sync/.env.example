# Postgres connection string — the same DB the main brackeys app uses.
DATABASE_URL=postgres://user:pass@host:5432/brackeys

# Sleep between accounts, ms — polite pacing against api.itch.io.
SYNC_DELAY_MS=500

USER_AGENT=brackeys-itchio-library-sync/0.1 (+https://brackeys.community)
