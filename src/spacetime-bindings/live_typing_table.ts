// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  type Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  type TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from '@clockworklabs/spacetimedb-sdk';
import type { LiveTyping } from './live_typing_type';
import { type EventContext, Reducer, RemoteReducers, RemoteTables } from '.';

/**
 * Table handle for the table `live_typing`.
 *
 * Obtain a handle from the [`liveTyping`] property on [`RemoteTables`],
 * like `ctx.db.liveTyping`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.liveTyping.on_insert(...)`.
 */
export class LiveTypingTableHandle {
  tableCache: TableCache<LiveTyping>;

  constructor(tableCache: TableCache<LiveTyping>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<LiveTyping> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `identity` unique index on the table `live_typing`,
   * which allows point queries on the field of the same name
   * via the [`LiveTypingIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.liveTyping.identity().find(...)`.
   *
   * Get a handle on the `identity` unique index on the table `live_typing`.
   */
  identity = {
    // Find the subscribed row whose `identity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): LiveTyping | undefined => {
      for (const row of this.tableCache.iter()) {
        if (deepEqual(row.identity, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: LiveTyping) => void) => {
    return this.tableCache.onInsert(cb);
  };

  removeOnInsert = (cb: (ctx: EventContext, row: LiveTyping) => void) => {
    return this.tableCache.removeOnInsert(cb);
  };

  onDelete = (cb: (ctx: EventContext, row: LiveTyping) => void) => {
    return this.tableCache.onDelete(cb);
  };

  removeOnDelete = (cb: (ctx: EventContext, row: LiveTyping) => void) => {
    return this.tableCache.removeOnDelete(cb);
  };

  // Updates are only defined for tables with primary keys.
  onUpdate = (
    cb: (ctx: EventContext, oldRow: LiveTyping, newRow: LiveTyping) => void,
  ) => {
    return this.tableCache.onUpdate(cb);
  };

  removeOnUpdate = (
    cb: (ctx: EventContext, onRow: LiveTyping, newRow: LiveTyping) => void,
  ) => {
    return this.tableCache.removeOnUpdate(cb);
  };
}
